import { CompactExerciseCard } from './CompactExerciseCard';
import type { ExerciseStage, WorkoutExercise, WorkoutRecommendation } from '../types/workout';

export function ExerciseStep({stepNumber,total,stage,candidates,recommendations,selectedId,onOpen,onSelect,onPrevious,onNext}:{stepNumber:number;total:number;stage:ExerciseStage;candidates:WorkoutExercise[];recommendations:Map<string,WorkoutRecommendation>;selectedId?:string;onOpen:(exercise:WorkoutExercise)=>void;onSelect:(exercise:WorkoutExercise)=>void;onPrevious?:()=>void;onNext?:()=>void}){return <section className="active-exercise-step"><header className="workout-step-header"><span>Шаг {stepNumber} из {total}{stage.optional?' · опционально':''}</span><h1>{stage.title}</h1><p>{stage.description}</p></header><div className="exercise-card-grid">{candidates.map(exercise=><CompactExerciseCard key={exercise.id} exercise={exercise} recommendation={recommendations.get(exercise.id)} selected={selectedId===exercise.id} onOpen={()=>onOpen(exercise)} onSelect={()=>onSelect(exercise)}/>)}</div><div className="step-actions">{onPrevious&&<button onClick={onPrevious}>Назад</button>}{onNext&&<button onClick={onNext}>Далее</button>}</div></section>}
